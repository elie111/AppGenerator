class SideBarParams {
    constructor(id, title, image, buttons) {
        this.id = id;
        this.title = title;
        this.image = image;
        this.buttons = buttons; // button params + newSectionSpace
    }
}

export default SideBarParams;