class HeaderParams {
    constructor(id, buttons, title, profileImage) {
        this.id = id;
        this.buttons = buttons;
        this.title = title;
        this.profileImage = profileImage;
    }
}

export default HeaderParams;