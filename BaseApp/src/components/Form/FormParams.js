class FormParams {
    constructor(targetId, action, actionInfo, dataSource, dataValue) {
        this.targetId = targetId;
        this.action = action;
        this.actionInfo = actionInfo;
        this.dataSource = dataSource;
        this.dataValue = dataValue;
    }
}

export default FormParams;